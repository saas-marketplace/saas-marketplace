-- Note: This seed file is for documentation/reference only
-- Admin users should be created through Supabase Auth
-- and then their role can be updated in the users table

-- Example of how to update an existing user's role to admin:
-- UPDATE users SET role = 'admin' WHERE email = 'admin@example.com';
