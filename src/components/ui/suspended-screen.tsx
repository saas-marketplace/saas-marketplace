// suspended-screen.tsx
// Previously redirected client-side and returned null (causing blank flash).
// Now unused by dashboard layout — layout does server-side redirect instead.
// Kept as a stub in case referenced from other places.
"use client";
export function SuspendedScreen() {
  return null;
}